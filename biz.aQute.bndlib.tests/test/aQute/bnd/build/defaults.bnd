# these really need to always be in defaults.bnd
src:          src
bin:          bin
testsrc:      test
testbin:      bin_test
target-dir:   generated

# Replaces the "real" defaults.bnd
# Used by WorkspaceTest.testPropertyDefaulting()
myprop1: defaults
myprop2: defaults
myprop3: defaults

# These properties must have the same values as biz.aQute.bndlib/src/aQute/bnd/build/defaults.bnd
javac.source: ${def;java.specification.version;1.8}
javac.target: ${def;java.specification.version;1.8}
