/*
 * Publish Gradle Plugin settings script
 */

rootProject.name = 'biz.aQute.bnd.gradle'
rootProject.setBuildFileName('publish.gradle')

gradle.rootProject {
  layout.buildDirectory.value(layout.projectDirectory.dir('generated'))
}
