import aQute.bnd.gradle.Resolve

tasks.named('resolve.resolvenochange') {
	failOnChanges = true
}
tasks.named('resolve.resolvechange') {
	failOnChanges = true
}

tasks.register('resolve2', Resolve) {
	dependsOn tasks.named('assemble')
	bndrun = file('resolve2.bndrunx')
}
