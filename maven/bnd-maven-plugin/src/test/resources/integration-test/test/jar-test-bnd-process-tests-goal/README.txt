This is an example bundle that provides a junit4 test which is built in to a
bundle using the bnd-process-tests goal and the maven-jar-plugin:test-jar goal.

Points of interest:

* The target folder should be populated with an artifact containing the
test class and the proper OSGi metadata for it.
