This repository directory is for build-only resources, or those that
must not be selected as bundles by a runtime resolver (e.g. OSGi Frame-
work implementations or Execution Environment specification JARs).